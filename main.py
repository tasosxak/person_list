from app.routes import app
"""
from app import app
import os

if __name__ == '__main__':
	port = int(os.environ.get('PORT', 8080))
	app.run(os.environ.get('DEBUG', False), host='0.0.0.0', port=port)
"""